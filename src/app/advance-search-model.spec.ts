import { AdvanceSearchModel } from './advance-search-model';

describe('AdvanceSearchModel', () => {
  it('should create an instance', () => {
    expect(new AdvanceSearchModel()).toBeTruthy();
  });
});
